(if
        (is (application_name) "gkrellm")
        (begin
                (undecorate)
                (skip_tasklist)
                (skip_pager)
                (below)
                (pin)
        )
)
