#################
# Start .muttsh #
#################

# * Echo the mutt configuration files in $HOME/.muttrc.d
# * Files in this directory must have '.muttrc' extension to be echoed
# * Files must also be prefixed by a two digit number. This number
#   specifies the order in which the files are echoed (lower numbered
#   prefixes being sourced first).
# * If a file does not conform to the above constraints, the results are
#   undefined.
for file in `ls $HOME/.muttrc.d/*.muttrc | sort -n`; do
	[ -r $file ] && cat $file
done
unset file

###############
# End .muttsh #
###############
